import globalReducer, { initialState as globalInitialState } from './Global/globalReducer';

export const initialStates = {
  globalReducer: globalInitialState
};

const reducers = {
  globalReducer
};

export default reducers;
